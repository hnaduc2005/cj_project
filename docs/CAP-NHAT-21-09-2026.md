# Cập nhật Excel ngày 21/09/2026

Đã thay danh mục hàng hóa/bảng giá hiện hành theo `D:\ThuCNU\CODE\Items list.xlsx`: 347 sản phẩm, 347 báo giá. Dữ liệu cũ không nằm trong danh sách mới chuyển INACTIVE, không xóa lịch sử hoặc mã tham chiếu trong đơn cũ. Email nhà cung cấp đã bổ sung trên web được giữ lại.

- 343 báo giá ACTIVE.
- 3 báo giá EXPIRED: Seal Unisto Axis Mini 300 (hết hạn 31/12/2025), Dây Rút 4x150 (31/03/2026), Màng PE 500 x 2.8kg x 300gL (18/09/2026).
- Sáp thơm Sara 75g có đơn giá 0 trong Excel: giữ nguyên giá 0, INACTIVE và chờ Admin xác nhận.
- VAT dạng `0.08` và `8 %` đều được chuyển thành 8%. “đang cung cấp” ở ngày kết thúc được hiểu là chưa có ngày kết thúc.
- Đã cập nhật 25 địa điểm từ WH List. Ô email trống trong Excel không xóa email đã được bổ sung trên website; địa điểm tạo riêng trên web vẫn được giữ.

Mẫu `FORM PO.xlsx` mới đã được áp dụng tại `templates/po-form.xlsx`. Mã/ngày PO ở B5/F5, nhà cung cấp B6, địa điểm B9, địa chỉ E9, mặt hàng bắt đầu dòng 19. VAT trong Excel PO được lưu dạng phần trăm. Các ô gộp, ghi chú và chữ ký được dịch đúng khi có trên 5 mặt hàng.

Đã kiểm thử PO 1, 5, 8 mặt hàng và toàn bộ 26 kiểm thử nghiệp vụ. 11 đơn hàng, 21 dòng hàng, 12 snapshot, 8 file PO và 9 bản ghi email được giữ nguyên số lượng khi cập nhật. Không phát hành hay gửi thêm PO thử trên dữ liệu thật.

Chi tiết và đường dẫn bản sao lưu: [COMPANY-UPDATE-REPORT.json](COMPANY-UPDATE-REPORT.json).

Bản Excel mới lưu tại `templates/items-current.xlsx` và `templates/warehouses-current.xlsx`. Các file nạp lần đầu từ phiên bản cũ vẫn được giữ riêng; website hiện sử dụng danh mục trong SQLite. Sửa Excel ngoài thư mục không tự đồng bộ. Script `scripts/update-company-files.mjs` mặc định chỉ kiểm tra; `--apply` thay danh mục và tạo sao lưu. Chỉ chạy khi chủ động muốn thay danh mục hiện hành bằng các file nguồn đó.

Mở website rồi nhấn Ctrl+F5. Mẫu mới áp dụng cho PO phát hành mới; PO đã phát hành vẫn giữ nguyên file lịch sử.
